# figma-styles-exporter

Simple program to generate Dart code from all Figma local styles.

## Sample

```bash
> dart cli/bin/cli.dart --token <token> --fileKey <filekey> --output test.dart --target flutter --output sample/lib/theme.dart
```
